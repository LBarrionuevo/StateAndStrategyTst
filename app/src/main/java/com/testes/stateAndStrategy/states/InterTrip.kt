package com.testes.stateAndStrategy.states

import android.view.View
import com.testes.stateAndStrategy.states.LayoutState

class InterTrip(
    label: String,
    onClick: View.OnClickListener
) : LayoutState(label, onClick) {

}